name: Deploy Preview on Feature PR

on:
  pull_request:

jobs:
  deploy:
    if: startsWith(github.head_ref, 'feature/')
    name: Deploy Preview
    environment: preview
    type: deploy
    params:
      preview: true
