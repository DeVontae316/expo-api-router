name: Deploy Preview on Feature PR

on:
  pull_request:
    branches:
      - feature/** # 👈 triggers when PR is opened from feature/*

jobs:
  deploy:
    name: Deploy Preview
    environment: preview # 👈 uses EAS Preview environment
    type: deploy
    params:
      preview: true # 👈 marks this as a preview deployment
