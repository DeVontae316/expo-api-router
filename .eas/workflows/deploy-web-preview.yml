name: Feature Preview Build
on:
  pull_request:
    branches:
      - "*" # match all branches
      - "!main" # except main

jobs:
  deploy:
    name: Deploy Preview
    environment: preview
    type: deploy
    params:
      preview: true
